# VisionGPT Frontend

React + Vite + TypeScript + Tailwind + shadcn/ui-style component setup.

## Run
```bash
cd frontend
cp .env.example .env
npm install
npm run dev
```

Frontend URL: `http://localhost:5173`

Set backend URL in `.env`:
- `VITE_API_BASE_URL=http://localhost:8000`
